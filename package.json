{
  "name": "goblin-mode",
  "displayName": "goblin mode — Discord rich presence for AI",
  "description": "Discord rich presence for AI coding. Friends see when Claude Code, Cursor, Codex, or Gemini is shipping for you — not when your cursor blinks.",
  "version": "0.3.2",
  "publisher": "leonardomjq",
  "author": { "name": "Leonardo Jaques", "url": "https://github.com/leonardomjq" },
  "bugs": { "url": "https://github.com/leonardomjq/goblin-mode/issues" },
  "homepage": "https://github.com/leonardomjq/goblin-mode#readme",
  "engines": { "vscode": "^1.93.0" },
  "main": "./dist/extension.cjs",
  "activationEvents": ["onStartupFinished"],
  "categories": ["Visualization", "Other"],
  "keywords": [
    "discord rich presence",
    "discord rpc",
    "discord presence",
    "discord",
    "rich presence",
    "claude code",
    "cursor",
    "ai coding",
    "ai agent",
    "claude",
    "codex",
    "gemini",
    "vibe coding",
    "rpc",
    "presence",
    "goblin mode",
    "status"
  ],
  "icon": "assets/icon.png",
  "capabilities": {
    "untrustedWorkspaces": { "supported": true },
    "virtualWorkspaces": false
  },
  "repository": { "type": "git", "url": "https://github.com/leonardomjq/goblin-mode.git" },
  "license": "MIT",
  "contributes": {
    "configuration": {
      "title": "Agent Mode",
      "properties": {
        "agentMode.clientId": {
          "type": "string",
          "default": "",
          "title": "Discord Client ID",
          "description": "Override the bundled Discord Application Client ID. Leave empty to use the default."
        },
        "agentMode.idleBehavior": {
          "type": "string",
          "enum": ["show", "clear"],
          "enumDescriptions": [
            "Render the IDLE copy pool on idle.",
            "Clear the Discord activity on idle (RPC stays connected)."
          ],
          "default": "show",
          "title": "Idle Behavior",
          "description": "What to do when no editor is focused and no agent is active."
        },
        "agentMode.activityType": {
          "type": "string",
          "enum": ["watching", "playing"],
          "enumDescriptions": [
            "Render activity as 'Watching X' (default — pattern interrupt, signals you're supervising the AI).",
            "Render activity as 'Playing X' (fallback for clients that do not render the Watching prefix correctly)."
          ],
          "default": "watching",
          "title": "Activity Type",
          "description": "Controls the Discord ActivityType prefix. Default 'watching' is the brand stance: you are watching the AI agent build. Switch to 'playing' if your Discord client does not render Watching correctly."
        },
        "agentMode.debug.verbose": {
          "type": "boolean",
          "default": false,
          "title": "Verbose Debug Logging",
          "description": "Enables detailed logging in the 'Agent Mode (Discord)' output channel."
        },
        "agentMode.animations.enabled": {
          "type": "boolean",
          "default": true,
          "title": "Enable frame animations",
          "description": "When false, multi-frame messages render their first frame statically."
        },
        "agentMode.messages.customPackPath": {
          "type": "string",
          "default": "",
          "title": "Custom Pack Path",
          "description": "Absolute path to a JSON copy pack. Falls back to built-in goblin pack if invalid."
        },
        "agentMode.privacy.filename": {
          "type": "string",
          "enum": ["show", "hide"],
          "enumDescriptions": [
            "Show the active filename verbatim.",
            "Replace the active filename with an empty string."
          ],
          "default": "show",
          "title": "Filename Privacy",
          "description": "Whether to expose the active filename in Discord presence."
        },
        "agentMode.privacy.gitBranch": {
          "type": "string",
          "enum": ["show", "hide"],
          "enumDescriptions": [
            "Show the current Git branch verbatim.",
            "Replace the Git branch with an empty string."
          ],
          "default": "show",
          "title": "Git Branch Privacy",
          "description": "Whether to expose the current Git branch in Discord presence."
        },
        "agentMode.privacy.workspaceName": {
          "type": "string",
          "enum": ["show", "hide", "hash"],
          "enumDescriptions": [
            "Show workspace name verbatim.",
            "Replace workspace name with empty string.",
            "Replace with 6-char SHA-1 prefix of the absolute path."
          ],
          "default": "show",
          "title": "Workspace Name Privacy",
          "description": "Whether to expose the workspace name in Discord presence."
        },
        "agentMode.ignore.gitHosts": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "title": "Ignored Git Hosts",
          "description": "Host names matched case-insensitively against the current repository's Git host; matches silence the extension entirely."
        },
        "agentMode.ignore.organizations": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "title": "Ignored Organizations (regex)",
          "description": "Regex patterns matched against the normalized host/owner segment of the current repository."
        },
        "agentMode.ignore.repositories": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "title": "Ignored Repositories (regex)",
          "description": "Regex patterns matched against normalized host/owner/repo."
        },
        "agentMode.ignore.workspaces": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "title": "Ignored Workspaces (glob)",
          "description": "Glob patterns matched against absolute workspace paths; matches silence the extension entirely."
        },
        "agentMode.detect.customPatterns": {
          "type": "object",
          "default": {},
          "title": "Custom agent regex patterns",
          "description": "User-defined regex → agent label mappings, added to the built-in detection set."
        },
        "agentMode.detect.sessionFileStalenessSeconds": {
          "type": "number",
          "default": 60,
          "minimum": 10,
          "maximum": 300,
          "title": "Session-file staleness threshold (seconds)",
          "description": "Seconds after which a Claude Code JSONL session file is considered stale and no longer signals an active agent."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs --production",
    "build:dev": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "check:bundle-size": "node scripts/check-bundle-size.mjs",
    "check:api-surface": "node scripts/check-api-surface.mjs",
    "check:no-network": "node scripts/check-no-network.mjs",
    "check:config-keys": "node scripts/check-config-keys.mjs",
    "check:pack-inlined": "node scripts/check-pack-inlined.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@xhayper/discord-rpc": "^1.3.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.118.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "overrides": {
      "vite@<6.4.2": ">=6.4.2",
      "postcss@<8.5.10": ">=8.5.10",
      "esbuild@<0.25.0": ">=0.25.0"
    }
  }
}
